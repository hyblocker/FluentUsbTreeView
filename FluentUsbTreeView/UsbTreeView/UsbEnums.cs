namespace FluentUsbTreeView {
    public enum UsbTreeIcon {
        Computer,
        HostController,
        UsbHub,
        NoDevice,
        GoodDevice,
        BadDevice,
        GoodSsDevice,
        NoSsDevice,
    }

    public enum DeviceInfoType {
        HostController,
        RootHub,
        ExternalHub,
        DeviceInfo
    }
}
